<% if @ebooks.any? %>
  <%= paginate @ebooks %>
  <table>
    <thead>
    <tr>
      <th></th>
      <th><%= ebook_sort_link('title', t('activerecord.attributes.user.first_name')) %></th>
      <th><%= ebook_sort_link('last_name', t('activerecord.attributes.user.last_name')) %></th>
      <th><%= ebook_sort_link('email', t('activerecord.attributes.user.email')) %></th>
      <th><%= ebook_sort_link('role', t('activerecord.attributes.user.role')) %></th>
      <th><%= ebook_sort_link('publisher_id', t('activerecord.attributes.user.publisher_id')) %></th>
      <th></th>
    </tr>
    </thead>
    <tbody id="users-list">
    <% @ebooks.each do |ebook| %>
      <%#= render partial: 'admin/users/user', locals: { user: user } %>
    <% end %>
    </tbody>
  </table>
  <%= paginate @users %>
<% else %>
  <p class="padding-10"><%= t('views.admin.users.no_users') %></p>
<% end %>
